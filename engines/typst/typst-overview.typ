#import "@preview/rubber-article:0.5.1": *

#show: article.with(
  heading-numbering: none,
  page-margins: 1.5in,
)

#set text(font: ("CMU Serif", "David CLM"), lang: "he")
#maketitle(
  title: "מנוע Typst",
)


= מה זה Typst ?

מדובר על תוכנה שונה לגמרי (לא מבוססת TeX) שעובדת עם מנוע מודרני ומהיר (כתוב ב--rust!) ומאפשרת כתיבה חלקה הרבה יותר. ה-syntax הרבה יותר ידידותי (מזכיר Markdown)  ועבודה עם עברית היא הרבה יותר straight--forward.

== דוגמא ל--תחביר:

```typst
= section
regular paragraph (אפשר גם בעברית)
- רשימה
  - לא  ממויינת
1. רשימה
2. ממויינת
$f(x)=x^2$ (מתמטיקה פנימית), $ f(x) = x^2 $ (מתמטיקה חיצונית)
*bold text*, _italic_text_, *_bold_italic_*, `inline code`
```
== יתרונות
לכל מי שרגיל ל-markdown, התחביר מדבר לעצמו. החיכוך בכתיבה יורד למינימום.
== חסרונות
- עדיין לא אומץ רשמית ע״י רוב האקדמיה
- אין את כל החבילות של LaTex (אבל יש הרבה חבילות דומות)
- צריך להתרגל לתחביר המתמטיקה השונה (כשמתרגלים זה להיט)
- *חסרון גדול*: מראה ברירת המחדל אינו תואם את המראה של LaTex (גופנים שונים, אין את ה-row-justification וכו), אפשר אבל בקלוט להגיע למראה דומה (כמו בדוגמא הנ״ל)

== מתי להשתמש ב--Typst?
בכל סיטואציה שבה אין צורך למראה שהוא 100% LaTex, ויש יתרון לכתיבה מהירה ונוחה.
